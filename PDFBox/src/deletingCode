
		// // logo sologo
		// PDImageXObject logo =
		// PDImageXObject.createFromFile("C:/Users/wojtek/Desktop/Apache PDF
		// tests/images/logo.png",
		// document);
		// float logoUpperLeftX= 119f;
		// float logoUpperLeftY= 27f;
		// contentStream.drawImage(logo, logoUpperLeftY*POINTS_PER_MM,
		// logoUpperLeftX*POINTS_PER_MM);

		// symbolName text settings
		

		// // create a link annotation
		// PDAnnotationLink txtLink = new PDAnnotationLink();
		//
		//// // add an underline
		//// PDBorderStyleDictionary underline = new PDBorderStyleDictionary();
		//// underline.setStyle(PDBorderStyleDictionary.STYLE_UNDERLINE);
		//// txtLink.setBorderStyle(underline);
		//
		// // set up the markup area
		// float offset = (font.getStringWidth("This is a link to ") / 1000) *
		// 18;
		// float textWidth = (font.getStringWidth("PDFBox") / 1000) * 18;
		// PDRectangle position = new PDRectangle();
		// position.setLowerLeftX(offset);
		// position.setLowerLeftY(upperRightY - 24f);
		// position.setUpperRightX(offset + textWidth);
		// position.setUpperRightY(upperRightY -4);
		// txtLink.setRectangle(position);
		//
		// // add an action
		// PDActionURI action = new PDActionURI();
		// action.setURI("http://www.pdfbox.org");
		// txtLink.setAction(action);
		//
		// // and that's all ;-)
		// page.getAnnotations().add(txtLink);
		//
		// document.save("C:/Users/wojtek/Desktop/Apache PDF tests/test2.pdf");
		// document.close();


	
		
		
		
		